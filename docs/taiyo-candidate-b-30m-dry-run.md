# 6976候補B・最大30分版 DRY_RUN監査手順

## 位置づけ

候補Bは、太陽誘電（6976）について**前場の10本終値ブレイクを1本確認して入る新方式**と、既存の**後場反転SHORT**を組み合わせたDRY_RUN専用ハイブリッドです。通常運用では旧朝初動SHORTと後場反転LONGを停止します。

> **LIVE注文は承認されていません。** クラウドの注文指示作成境界とWindows Executor/relay参照版の双方で、候補BのLIVE新規注文を明示的に拒否します。決済注文は安全のため止めません。Windows参照版は引き続き `DRY_RUN = True` です。

## 固定仕様

| 項目 | 仕様 |
|---|---|
| 対象 | 6976 太陽誘電 |
| 当日始値 | 09:00以降の最初の保存1分足の始値。08:59足は除外 |
| 初動時間 | 09:45〜10:59 |
| 確認時間 | 次の1分足。最終確認は11:00 |
| LONG初動 | 終値が直前10本高値を上抜く陽線、当日始値より上、MA8二本傾き+0.05%以上 |
| SHORT初動 | 終値が直前10本安値を下抜く陰線、当日始値より下、MA8二本傾き-0.05%以下 |
| 出来高 | 初動足が直前20本平均の1.0倍以上 |
| 1本確認 | LONGは確認足終値が初動終値より上かつ陽線、SHORTは下かつ陰線 |
| エントリー | 確認足終値 |
| 日次回数 | primaryは実エントリー成功時だけ1回を消費 |
| TP / SL | TP 0.6% / SL 1.0% |
| 最大保有 | 30分。境界の完成1分足終値で因果的に決済 |
| 前場終了 | 11:27時点で未決済なら既存の前場強制決済 |
| その他出口 | シグナル反転・板読み早期利確・利益保護は使用しない |
| 既存経路 | 後場反転SHORTのみ維持。旧朝SHORTと後場LONGは停止 |

## 拒否後の再探索

初動後の確認足が不成立なら、同じ確認足を新しい初動候補として再評価します。確認が成立しても共通エンジンのATR、証拠金、対象銘柄などのゲートで拒否された場合は、primaryの日次枠を消費しません。次の確定足以降も候補探索を継続し、**実エントリー成功時だけ**発火済みとします。

証拠金拒否は既存の `margin_block`、ATR等の拒否は `candidate_b_block` として当日のメモリー上のシグナル履歴へ残します。加えて、確認失敗とATR・証拠金等の共通ゲート拒否を専用の `rt_taiyo_candidate_b_events` テーブルへ冪等保存します。16時の日次通知はDBイベントも読み込むため、通知前にサーバーが再起動しても拒否・再探索履歴を復元できます。

## Git固定データと期待値

入力は `server/fixtures/taiyoCandidateA.audit.fixture.json` に固定したKABUステーション保存データです。候補A/Bは同じ一次データを共有します。

| 項目 | 固定値 |
|---|---:|
| 保存営業日 | 46日 |
| 1分足 | 14,719本 |
| fixture SHA-256 | `8ba72c0fbdb043135bf3c8677e82a2205a6b1f644c958e0d49ac7e6cda3c244e` |
| 全取引 | 33件（primary 30件、後場SHORT 3件） |
| 勝敗 | 28勝5敗 |
| 勝率 | 84.85% |
| 100株損益 | +157,872.01円 |
| 確認失敗 | 42件 |
| 証拠金無制限100株評価の共通ゲート拒否 | 0件 |
| 直近5完全日 | 5件・5勝0敗・+22,963.34円/100株 |

保存ID順・7銘柄・元金300万円・総エクスポージャ891万円・可変株数の統合ソース監査では、93,072足から214件・164勝50敗・+3,831,483円を再現します。この値は単独100株評価とは別の資金制約付き評価です。

全33取引の日付、経路、方向、入口時刻・価格、出口時刻・理由、100株損益は `server/fixtures/taiyoCandidateB.expected.ts` に固定しています。確認失敗イベント列は正規化SHA-256でも固定し、取引だけが偶然一致する状態を防ぎます。

fixtureは `.gitattributes` で `text eol=lf` を指定しています。WindowsチェックアウトでもCRLF変換を受けず、生バイトSHA-256が同じ値になります。MA8二本傾き±0.05%は今回追加した条件ではなく、候補Bの残存原検証コードに含まれていた既存条件です。

## 再監査コマンド

DBなしで、Gitだけから仕様境界、全46日、全33取引、拒否後再探索、再起動リスク復元、日次乖離表示を確認できます。

```bash
pnpm vitest run \
  server/taiyoCandidateB.spec.test.ts \
  server/taiyoCandidateB.auditReplay.test.ts \
  server/taiyoCandidateBDryRunReport.test.ts \
  server/realtimeSimRestoration.test.ts \
  server/orderBridge.test.ts
```

全体非回帰は次で確認します。

```bash
pnpm vitest run
pnpm check
pnpm build
```

## 前向きDRY_RUNで比較する項目

日次通知と保存取引を使い、理論上の確認足終値と実際の受信・判定時刻、証拠金またはATR拒否後の次候補、30分境界の確定足価格、TP/SL到達、資金競合で変化した他銘柄を確認します。DRY_RUN価格は実約定ではないため、手数料、金利、スリッページ、部分約定、約定不能は別途評価が必要です。

最低10〜20シグナルを前向き観察するまではLIVE採用判断を行いません。TP 0.6%・SL 1.0%でTP＜SLである点も、従来方針との差として明示的に保留します。
